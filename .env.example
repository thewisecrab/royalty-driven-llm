# Copy to .env or inject through your platform secret manager.
# This is a SHA-256 hash of the bearer token, not the raw bearer token.
RDLLM_SERVICE_TOKEN_SHA256=replace-with-64-character-sha256-token-hash
